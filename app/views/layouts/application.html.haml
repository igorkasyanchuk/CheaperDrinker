%html
  %head
    %title= yield :title
    <link  href="//fonts.googleapis.com/css?family=PT+Sans:regular,italic,bold" rel="stylesheet" type="text/css" />
    = stylesheet_link_tag 'ui-lightness/jquery-ui-1.8.6.custom', 'application', 'colorbox', :cache => 'application_bundle'
    = yield :css
    = yield :js
    = javascript_include_tag 'jquery-1.4.4.min', 'jquery-ui-1.8.6.custom.min', 'rails.jquery', 'jquery.colorbox-min', 'mapiconmaker_packed', 'jquery.raty.min.js', :cache => 'application_bundle_static'
    = javascript_include_tag 'z', :cache => 'application_bundle'
    = csrf_meta_tag
    = yield :map
    = yield :markitup
    %script{:type => 'text/javascript', :charset => 'utf-8'}
      $(document).ready(function() {
      init_header_autocomplete();
      = yield :on_ready
      });
      = yield :inline_js
    = include_google_analytics
    = add_admin_nav
  %body
    #root
      #content
        .container= raw(flash_messages)
        .container.header_container= render '/shared/header'
        .container
          .content_container= yield
        .clear
      #root_footer
    #footer
      = render :partial => '/shared/footer'
      = render :partial => '/shared/user_voice'