= title "Add Bar"

%h1 Add Bar

= content_for :on_ready do
  init_add_form(); 
  
= error_messages_for :location

= simple_form_for @location, :url => [:dashboard, :create_location] do |f|
  = render :partial => '/dashboard/locations/inputs', :locals => { :f => f }
  = f.submit 'Create'