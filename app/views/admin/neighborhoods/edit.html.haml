= title "Edit: #{@neighborhood.name}"

%h1.left== Edit: #{@neighborhood.name}
.add.right
  = link_to 'Back', [:admin, @market.state, @market, :neighborhoods], :class => 'cancel_button'
.clear
= render 'form'